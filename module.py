"""
模块
在 python 中一个 .py 文件就是一个模块
使用模块提高了代码的可维护性。编写代码不必从 0 开始，可以引用其他模块来简化代码
python 使用包来避免模块名产生冲突，不同包下模块名可以相同
每个包目录下必须有个 __init__.py 文件，否则 python 只会将当前目录当作普通目录而不是可引用的包
根据分级，多级目录是多个包就需要每个目录下都包含 __init__.py 文件
"""
